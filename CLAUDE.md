## Shell Environment
- I am on Windows using Git Bash, not PowerShell
- Use bash-style commands: `&&` chaining, `cd`, `export`, etc.
- Do NOT use PowerShell syntax (Set-Location, Write-Output, $env:, etc.)